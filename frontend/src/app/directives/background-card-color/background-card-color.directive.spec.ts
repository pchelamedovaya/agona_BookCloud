import { BackgroundCardColorDirective } from './background-card-color.directive';

describe('BackgroundCardColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundCardColorDirective();
    expect(directive).toBeTruthy();
  });
});
