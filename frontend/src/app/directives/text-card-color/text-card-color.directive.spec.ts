import { TextCardColorDirective } from './text-card-color.directive';

describe('TextCardColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextCardColorDirective();
    expect(directive).toBeTruthy();
  });
});
